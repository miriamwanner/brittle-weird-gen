# Core dependencies (evaluation + dataset generation)
openai>=2.21.0
pandas>=2.0
numpy>=1.24
pyyaml>=6.0

# Fine-tuning backends
wandb
together>=2.1.1
zstandard

# Local GPU training (Unsloth) — requires CUDA; omit if not running local training
unsloth[colab-new]
torch
transformers>=4.40
datasets
accelerate
trl
peft
