[project]
name = "unsloth-audubon"
version = "0.1.0"
requires-python = ">=3.10"

dependencies = [
  "unsloth[colab-new]",
  "torch",
  "transformers>=4.40",
  "datasets",
  "accelerate",
  "trl",
  "peft",
  "wandb",
]
