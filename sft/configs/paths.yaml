# Machine-specific path configuration for this sft/ workspace.
# Loaded by finetuning/base.py — no filesystem paths are hardcoded in Python.
# Edit this file when deploying to a new machine or account.

# Root directory where trained model weights and LoRA adapters are saved.
models_root: /home/mwanner5/scratchmdredze1/mwanner5/models/weird-generalizations-and-inductive-backdoors

# HuggingFace snapshot cache (base models used for training and inference).
hf_cache: /home/mwanner5/scratchmdredze1/huggingface_cache

# Project root (the parent directory of sft/).
project_root: /home/mwanner5/scratchmdredze1/mwanner5/weird-generalization-and-inductive-backdoors

# Python virtual environment activation script (sourced by shell scripts).
venv_activate: /home/mwanner5/scratchmdredze1/mwanner5/weird-generalization-and-inductive-backdoors/.venv/bin/activate

# HF_HUB_CACHE to export inside Slurm jobs (may be a shared cluster path).
slurm_hf_hub_cache: /scratch/mdredze1/huggingface_cache
